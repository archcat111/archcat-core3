package org.cat.support.db3.generator.druid.study;

public class DruidDataSourceJianKong {
	//	private void test(DruidDataSource druidDataSource) {
	//	druidDataSource.getActiveCount();//当前正在被使用的连接数量，这部分已经被用户拿去使用还没归还
	//	druidDataSource.getPoolingCount();//当前连接池中数据库连接的数量
	//	druidDataSource.getPoolingPeak();//池的最高峰数量
	//	druidDataSource.getPoolingPeakTime();//池的最高峰数量的时间
	//	//ActiveCount+PoolingCount=druid 拥有的所有连接数量
	//	druidDataSource.getActivePeak();//activeCount的峰值
	//	druidDataSource.getActivePeakTime();//活跃最高峰时间
	//	druidDataSource.getConnectCount();//获取连接的次数
	//	druidDataSource.getCloseCount();//关闭连接数量
	//	druidDataSource.getNotEmptyWaitCount();//阻塞等待线程数量，累计，表示当前等待可用连接时阻塞的业务线程的总个数
	//	druidDataSource.getWaitThreadCount();//当前等待获取连接的线程数(最后一次更新统计数据时的数据)
	//	druidDataSource.getTransactionHistogramRanges();//事务运行分布时间[0-10 ms, 10-100 ms, 100-1 s, 1-10 s, 10-100 s, >100 s]
	//	druidDataSource.getTransactionHistogramValues();//事务运行分布时间区间对应的数量
	//	druidDataSource.getExecuteCount();//执行SQL数量
	//}
}
